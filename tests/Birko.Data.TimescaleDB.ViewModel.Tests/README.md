# Birko.Data.TimescaleDB.ViewModel.Tests

xUnit + FluentAssertions tests for `Birko.Data.TimescaleDB.ViewModel`
(`AsyncTimescaleDBRepository`). Offline — constructor store-type guard, SetSettings routing (Connector
availability), and the "Connector not initialized" lifecycle guards. No live database required.
